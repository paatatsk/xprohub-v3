TSC BASELINE — RESOLVED 2026-05-03

All 24 errors that were captured in this baseline have been resolved.
See commit history for fixes (Task 2 completion).

Fix summary:
  - 16 Edge Function errors: excluded supabase/functions/** from app tsconfig
    (Deno tooling setup tracked as Task 3 in PROJECT_STATUS)
  - 6 mechanical casts: added `as any` on dynamic route strings (3 errors),
    added `as unknown as` on Supabase join type mismatches (3 errors)
  - 1 gestureEnabled no-op: removed broken property from Tabs.Screen,
    added proper BackHandler interception to apply-success.tsx
  - 1 segments.length check: widened comparison from `segments.length === 0`
    to `!segments[0]` to satisfy Expo Router tuple typing

This file is retained for historical reference. Safe to delete in
a future cleanup pass.
